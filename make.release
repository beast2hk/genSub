VPATH=$(CURRPATH)/$(SRCPATH)
COMMON_FLAGS+= -DRELEASE -O3
CPPFLAGS+= $(COMMON_FLAGS)

all: $(TARGET) 

$(TARGET): $(GCH_OBJ) $(OBJS) $(TARGET_OBJS)
	$(CXX) -o $@ $(OBJS) bin/$@.o $(CPPFLAGS) $(LDFLAGS)
